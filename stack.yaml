resolver: lts-12.0
packages:
- core/
- hunit/
- quickcheck/
- smallcheck/
- core-tests/
extra-deps:
- wcwidth-0.0.2
# this is needed for travis, because call-stack is absent from the old snapshots
- call-stack-0.1.0
flags: {}
